- args:
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    sql: CREATE TABLE "staging"."cases"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "guild" text NOT NULL, "message" text, "case_id" integer NOT NULL, "ref_id"
      integer, "target_id" text NOT NULL, "target_tag" text NOT NULL, "mod_id" text, "mod_tag" text,
      "action" integer NOT NULL, "reason" text, "action_duration" timestamptz,
      "action_processed" boolean DEFAULT true, "created_at" timestamptz NOT NULL DEFAULT now(),
      PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: cases
    schema: staging
  type: add_existing_table_or_view
