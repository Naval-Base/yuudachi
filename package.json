{
	"name": "@yuudachi/yuudachi",
	"version": "0.0.0",
	"description": "",
	"scripts": {
		"build": "yarn workspaces foreach --parallel --topological --since=origin/main run build",
		"test": "yarn workspaces foreach --parallel --topological --since=origin/main run test",
		"lint": "yarn workspaces foreach --parallel --topological --since=origin/main run lint",
		"format": "yarn workspaces foreach --parallel --topological --since=origin/main run format",
		"fmt": "yarn format",
		"postinstall": "is-ci || husky install",
		"update": "yarn upgrade-interactive"
	},
	"type": "module",
	"keywords": [],
	"author": "iCrawl <icrawltogo@gmail.com>",
	"license": "AGPL-3.0",
	"private": true,
	"devDependencies": {
		"husky": "^8.0.2",
		"is-ci": "^3.0.1",
		"lint-staged": "^13.0.4",
		"typescript": "^4.9.3",
		"vercel": "^28.5.6"
	},
	"engines": {
		"node": ">=16.0.0"
	},
	"workspaces": [
		"packages/*"
	],
	"packageManager": "yarn@3.2.4"
}
